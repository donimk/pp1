#include <iostream>
#include <queue>
using namespace std;

int main() {
    queue<int> myQueue;
    int num;

    cout << "Enqueue:" << endl;
    for (int i = 0; i < 5; ++i) {
        cin >> num;
        myQueue.push(num);
    }

    cout << "Dequeued:" << endl;
    while (!myQueue.empty()) {
        cout << myQueue.front() << endl;
        myQueue.pop();
    }

    return 0;
}

